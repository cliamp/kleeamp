/// Stable product identity shared by every client. The display name is
/// lower-case on purpose: the app is `cliamp`, not `Cliamp`.
public enum CliampIdentity {
    public static let displayName = "cliamp"
    public static let bundleIdentifier = "stream.cliamp.mobile"
}
